# Required
JWT_SECRET=          # openssl rand -hex 32
GOOGLE_CLIENT_ID=    # GCP Console → OAuth 2.0 Client IDs

# Set by fly.toml [env] in production
PORT=3001
NODE_ENV=production
DB_PATH=/data/jobtracker.db
LOG_PATH=/data/app.log

# Optional
ALLOWED_ORIGINS=https://eichhorn-personal.github.io
ADMIN_EMAIL=         # Gets admin role automatically on login
ALLOWED_EMAILS=      # Comma-separated; unset = open registration
